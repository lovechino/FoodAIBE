[phases.install]
cmds = [
  # Cài PyTorch CPU-only trước (~300MB thay vì ~2.5GB với GPU)
  "pip install torch==2.2.0 --index-url https://download.pytorch.org/whl/cpu",
  "pip install -r requirements.txt"
]

[start]
cmd = "uvicorn api.main:app --host 0.0.0.0 --port $PORT"
